name: my-app

build:
  env:
    NODE_ENV: production
  plugins:
    - name: '@railway/static-build'
      env:
        SKIP_CACHE: true
  script:
    - pnpm install --frozen-lockfile

deploy:
  postbuild:
    - pnpm install eslint-plugin-bot-whatsapp@latest --save-dev
